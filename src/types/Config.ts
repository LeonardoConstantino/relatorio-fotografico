/**
 * Configurações globais de identidade do relatório
 */
export interface ReportConfig {
  logo: string; // Base64 ou URL
  title: string;
  primaryColor: string;
  company: string;
  reportNumber: string;
  reportDate: string;
}
